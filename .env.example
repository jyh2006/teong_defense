PORT=3000
NODE_ENV=development
# Production: replace with your real HTTPS origin (no trailing slash).
APP_ORIGIN=http://localhost:3000
DATA_DIR=./data
# Optional. Never commit a real key. Without it, deterministic explanations remain available.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
